{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "windows-only",
  "description": "Windows-only capabilities",
  "windows": ["main"],
  "platforms": ["windows"],
  "permissions": [
    "autostart:allow-enable",
    "autostart:allow-disable",
    "autostart:allow-is-enabled"
  ]
}
